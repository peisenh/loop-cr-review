<!--
  A plain platform theme on purpose. The activity is a WebView container and
  does not use AppCompat, so it must not inherit from Theme.AppCompat either -
  mixing the two throws "You need to use a Theme.AppCompat theme" on start.
-->
<resources>
    <style name="AppTheme" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:colorAccent">#555555</item>
        <item name="android:navigationBarColor">#000000</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">#ffffff</item>
    </style>
</resources>
