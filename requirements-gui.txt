# Desktop GUI with Qt WebEngine (pywebview[qt] + PyQt6).
# Used for Linux binaries and the full Windows Qt build.
#   pip install -r requirements-gui.txt   &&   python3 gui.py
# Slim Windows (WebView2, no Qt): requirements-gui-webview2.txt
#
# (Deliberately does NOT depend on gunicorn — it can't install on Windows.)
-r requirements.txt
Flask>=3.0,<4.0
pywebview[qt]>=5.0
waitress>=3.0
