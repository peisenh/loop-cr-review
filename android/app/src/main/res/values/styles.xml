<?xml version="1.0" encoding="utf-8"?>
<!--
  Plain platform theme on purpose. No AppCompat: mixing Theme.AppCompat
  with Activity() throws on start.

  Theme.Material.DayNight does not exist on the platform. Night is
  values-night/ below (Theme.Material is the dark Material theme).
  WebView reads isLightTheme from whichever style is loaded.
-->
<resources>
    <style name="AppTheme" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:colorAccent">#555555</item>
        <item name="android:navigationBarColor">#000000</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">#ffffff</item>
    </style>
</resources>
