# Python
__pycache__/
*.pyc
*.pyo
.venv*/
venv/

# Emacs
*~
\#*\#
.\#*
*.elc
TAGS

# PyInstaller / build artifacts
build/
dist/
*.spec
smoke.html

# generated reports (example PNGs in docs/ are kept)
*_loop-cr-review_*.html

# own exports / real data — NEVER commit (PHI)
/data/*
!/data/README.md

# own slot configuration (any name, e.g. slots-school.json,
# slots-vacation.json) — only the template in example-data/ is versioned
/slots*.json

# generated gettext template (regenerated by pybabel extract)
locale/messages.pot
docker-compose.yml
.env
build-version.bak

# profiling
profile.prof

# matplotlib Android wheel rebuild scratch
.mpl-android-build/
